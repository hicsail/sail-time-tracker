mutation addComment($input: CommentCreateInput!) {
    addComment(input: $input) {
        content
        commentId
        createDate
        invoiceId
    }
}

mutation deleteComment($id: String!) {
    deleteComment(id: $id) {
        commentId
    }
}